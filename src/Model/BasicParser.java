package Model;

import java.util.List;

public class BasicParser implements Parser {
    /**
    private Student createStudent(List<String> keys, List<String> values){
        Student tmp = new Student(values.get(0), values.get(1), values.get(2));
        for (int i = 3; i < keys.size(); i++){
            tmp.setAttribute(keys.get(i), values.get(i));
        }
        return tmp;
    }
    // parse the basic attributes that always should be given
    public void tmp(List<List<String>> students, List<String> keys){
        DataBase db = new DataBase();
        for (int i = 0; i < students.size(); i++){
            db.addStudent(createStudent(keys, students.get(i)));
        }
    }
     */
}
